# ─── LOCAL DEVELOPMENT ───────────────────────────────────────────────────────
# This file is gitignored. Never commit real credentials.

# DATABASE
spring.datasource.url=jdbc:postgresql://localhost:5433/batchflow
spring.datasource.username=postgres
spring.datasource.password=Aqueeb@31
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA – allow schema auto-update locally
spring.jpa.hibernate.ddl-auto=update

# JWT CONFIGURATION
jwt.secret=batchflow-dev-secret-key-min-32-chars-required
jwt.expiration-ms=86400000

# CORS – allow local frontend dev server
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
